PORT=4004

# REQUIRES: a live exchange-rate API. Pick a provider (exchangerate.host,
# openexchangerates.org, currencylayer, etc.) and set both - src/lib/currency.js
# is written against a generic "base URL + API key query param" shape typical
# of these providers; adjust the query param name/response field to match
# whichever you pick.
EXCHANGE_RATE_API_BASE_URL=
EXCHANGE_RATE_API_KEY=
