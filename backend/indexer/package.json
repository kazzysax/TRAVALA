{
  "name": "@travala/backend-indexer",
  "version": "0.1.0",
  "private": true,
  "description": "Listens to ServiceRating's RatingSubmitted events and aggregates them into a queryable store for the Recommendations feed.",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ethers": "^6.14.0",
    "dotenv": "^17.4.2"
  }
}
