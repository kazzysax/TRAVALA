PORT=4006
MONAD_RPC_URL=
SERVICE_RATING_ADDRESS=

# Block to start backfilling RatingSubmitted events from - use the contract's
# deployment block (printed by contracts/hardhat/scripts/deploy.js's receipt,
# or looked up on the block explorer) so the indexer doesn't scan from genesis.
START_BLOCK=0
